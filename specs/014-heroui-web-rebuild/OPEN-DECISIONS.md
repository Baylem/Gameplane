# Open decisions — feature 014

Values here are not traceable to `spec.md`, `CLAUDE.md` or the constitution. Each was a question for the maintainer, not a decision, until ruled on. Ruled rows are now enforced in CI and written into contracts/tasks as settled, per the ruling recorded in each row; open rows await maintainer decision.

| ID | Question | Ruling | Status |
|---|---|---|---|
| OD-1 | Public share-link route path in the dashboard. The API serves `GET /shares/{token}` and `POST /shares/{token}/start`; the web route is undefined. | Public share route path is `/share/$token`. | Settled 2026-09-03 |
| OD-2 | Where the light/dark/system appearance toggle lives (FR-006). No design frame shows one. | The appearance toggle lives in the sidebar profile footer next to logout, mirrored in the mobile navigation drawer; slice 1 adds a design frame for it. Refined 2026-09-05 (maintainer): the footer is two rows — the appearance toggle on its own row directly above the user-info + logout row, pill on $surface/secondary; same in the mobile drawer. | Settled 2026-09-03 |
| OD-3 | Constitution Principle I says every feature's E2E test lives in `test/e2e/` with a bucket. The dashboard's existing browser E2E is Playwright in `web/e2e/` (CI jobs `web-e2e-mock`, `e2e-web-live`), and the Go suite never drives a browser. | `web/e2e/` Playwright live specs are the E2E tier for this feature; no Go `test/e2e/` test is added. The constitution wording will be amended separately to name `web/e2e/`. | Settled 2026-09-03 |
| OD-4 | The imported HeroUI sample frame `dashboard-utility` (`bJ2cg`) and its prompt node `OFfAu` sit in `design.pen` beside real screens. | Delete the HeroUI sample frame `dashboard-utility` (`bJ2cg`) and its prompt node `OFfAu` from `design.pen` via the Pencil MCP, in slice 0; the maintainer saves in the GUI. | Settled 2026-09-03 |
| OD-5 | Whether `heroUI template.pen` (repo root, untracked, 391 KB) is committed. | `heroUI template.pen` is NOT committed; add it to `.gitignore` next to `design.pen.bak`. | Settled 2026-09-03 |
| OD-6 | CSS bundle budget after `@import "@heroui/styles"` (root import pulls every component's styles). | No CSS bundle budget; the root `@import "@heroui/styles"` stays regardless of size. The measure-and-switch task is dropped. | Settled 2026-09-03 |
| OD-7 | Name of the directory holding the rebuilt Gameplane compositions while `components/ui/` still exists. | Rebuilt compositions live in `web/src/components/hero/` during the transition and are renamed to `web/src/components/ui/` in slice 5, after the old primitives are deleted. | Settled 2026-09-03 |
| OD-8 | Keep today's orange brand and current surface/border/muted scale on HeroUI, or re-theme the dashboard? | Pink accent (`#FF4FA3` dark / `#DB2777` light), obsidian dark canvas (`#121114`), white-and-pink light page (`#FFFFFF` with `#FFF7FB` card surface and `#EFC3D6` borders). Complete token tables defined in `contracts/theme-tokens.md`. | Settled 2026-09-04 |
| OD-9 | Stat-card icons are hardcoded `#8B5CF6` (purple); add a HeroUI semantic token (e.g. `$chart/series` or icon variable) or keep the direct hex reference as `--color-violet`? | Formalize it as a dedicated HeroUI semantic token, not `--color-violet` and not a hardcoded hex: `--chart-stat` in `globals.css` (`oklch(60.56% 0.2189 292.72)` / `#8B5CF6`, same value in light and dark) aliased to `--color-chart-stat` in the `@theme` block, and `$chart/stat` in `design.pen`. `--color-violet` is unchanged and stays reserved for operator-role chips per `data-model.md`. Scoped to the stat-card icon fill only (`ZWcwn` component's `QdcEm` slot); other `#8B5CF6` uses found in `design.pen` (Wizard Stepper step indicator, Detail Header "AI" badge, public Share Card status badge, modpack cover swatch) are out of scope for this ruling. | Settled 2026-09-16 |
| OD-10 | Every screen root frame in `design.pen` still carries the lunaris theme axis `"c:Mode": "Dark"` beside `"semantic": "dark"` — the last `c:`-prefixed token on the nodes; it dangles once the c:Mode axis leaves the document (found by the slice-5 design review). | Drop `c:Mode` from every screen root frame document-wide in one blind edit pass (keep `semantic`), after the slice 2a–5 design reviews are clean; re-export the touched screens. | Settled 2026-09-06 |
| OD-11 | `Screen/Server Detail — Settings · Share links (Empty)` (`dQV9N`) keeps a full `enabled:false` copy of the three-row share-links table beside its empty state; it was rebuilt on HeroUI and will drift from the real table on `xCJlu`. | Delete the disabled table subtree from `dQV9N` and re-export; `xCJlu` is the single source for the table. | Settled 2026-09-06 |
| OD-12 | Several original screens carry literal placeholder buttons labelled "Button" (Create Server step 4 add-row, Backup Detail Drawer header action, Players row action, per-mod actions on Mods by ID and beside Save changes). Faithful re-skin keeps them; reviews keep flagging them. | Give them real labels in the re-skin: "Add port override" (plus icon), "Remove" on each selected-mod row and "Discard" beside "Save changes" on Mods by ID, "Restore" (rotate-ccw) on the Backup Detail Drawer header, "Refresh" (refresh-cw) on the Players online row. The last two names were chosen by the agent from the screen's function — maintainer may rename. | Settled 2026-09-06 |
| OD-13 | The rebuilt `Gameplane/Server Detail Tabs` (`I9kvlZ`) grew from 88 px to 128 px with the HeroUI Tabs pill, shifting every detail screen's vertical rhythm. | Keep the HeroUI pill height (128 px); reviews stop flagging it. | Settled 2026-09-06 |
| OD-14 | Screen/Create Server — Step 5 Review (UMJli): the original Create server button shows label then a trailing check icon; HeroUI's Button definitions only have a leading-icon slot and LtgNm must not be edited. Options: no icon / leading check icon / new Gameplane trailing-icon Button variant. | Leading check icon — keep the HeroUI Button/Primary/LG instance, un-hide its leading icon set to check. | Settled 2026-09-06 |
| OD-15 | Light-mode `--background` is `#FFFFFF`; may later become a very light pink following the obsidian dark direction. | OPEN — keep `#FFFFFF` for now; components reference `$background/background` so a later change propagates. | Open |
| OD-16 | Focus ring colour (earlier PR #378 ruling "focus ring purple"). | Superseded: the input/control focus ring uses the theme accent (`--accent`), not purple; `--focus` stays for the Asleep status indicator. Screenshot captures show no focus ring at rest (dialogs don't auto-focus their first input), except Reset password (MaoHP), whose capture focuses the password input because the design draws it focused (round 6, 2026-09-19; OD-22). Inside a dialog a focused input shows the accent as a 2px border drawn inside the field (1px border plus `inset 0 0 0 1px var(--accent)`), not an outer ring the dialog body would clip (round 7, 2026-09-19). | Settled 2026-09-16; refined 2026-09-19 |
| OD-17 | Button radius exceptions (ruling "all buttons 6px except Sign in / Create server (pill)"). | Modpacks tab buttons are also pill-shaped; the mobile Servers filter trigger takes the design's $radius/xl (`.button.filter-trigger`, maintainer sign-off 2026-09-19); all other buttons stay 6px, including the Role editor (CqaSq) and Backup drawer (zhLZN) footer buttons (round 6, 2026-09-19). Cancel buttons use the ghost variant (design Gameplane/Button/Ghost) at full strength in every dialog, never dimmed; the primary button's disabled look is unchanged (round-6 unified answer, 2026-09-19). | Settled 2026-09-16; refined 2026-09-19 |
| OD-18 | Game icon when a template has no image. | Use GameTemplate spec.icon (URL/data URI) when set; otherwise a unique two-letter code: first letter + the first following letter giving an unused code, assigned by install order (creationTimestamp ascending — the oldest template keeps its code), falling back to first letter + digit; tinted by accentColor. | Settled 2026-09-16 |
| OD-19 | Shared dialog shell geometry (Gameplane/Modal x3beP, confirm dialog WwNlX). HeroUI's default modal padding (~24px) stacked with `.modal__header`'s own top padding, so titles sat low and footers ran wide; the confirm dialog had no width or radius pin; the design draws a 1px stroke the app lacked. | Every Modal (`web/src/styles/globals.css`): 20px padding, gap 0; header padding 0 with a 4px title-to-description gap; body 16px top padding, 16px gap, margin 0; footer 20px top padding, right-aligned, 8px gap, margin 0. Confirm dialog (`AlertDialogDialog` in `web/src/components/ui/ConfirmDialog.tsx`): 440px wide, 12px radius, 20px padding, 12px header-icon gap. Every Modal and AlertDialog shows a 1px `var(--border)` outline at `outline-offset: -1px` (inside the box, so content does not move and HeroUI's elevation shadow stays) and a semibold (600) heading. | Settled 2026-09-19 (geometry round 5, outline and headings round 7) |
| OD-20 | Dialog field and footer typography (x3beP dialogs MaoHP, NLDDv, CqaSq, DMnEi, t3IY3u, E9EEv0; confirm dialogs WwNlX/Kp48V). | Inputs and selects inside a Modal are 36px tall with 12px corners (Gameplane/Input D0cDM, Select AT7ya) and 11px horizontal padding; every dialog select uses the design's smaller chevron (14px indicator, inset about 15px). Reset password, Invite user, Role editor, Add module source and Edit user take the design's title/description line heights and a 6px label-to-field gap; select values are vertically centred in the placeholder tone. Footer button labels are 12px in Reset password, Invite user, Add module source, Edit user and Restore backup; confirm-dialog footer labels stay 14px. Focus treatment: OD-16. | Settled 2026-09-19 (round 6; padding and chevron round 7) |
| OD-21 | Per-dialog rulings from visual-diff rounds 5-7. | Add module source (DMnEi): 480px wide like every x3beP dialog (was 544px); labels 12px muted regular, helpers 11px muted, checkbox text in the foreground colour, "Allow plain HTTP" at regular weight; "Add source" disabled until the form is valid (`canSubmit` mirrors `submit()`'s checks). Edit user (t3IY3u): no "No per-namespace grants." empty-state line; the grant role picker is the styled Select (not a native select), 32px tall, defaulting to "operator"; grants label 14px line height; grant Add label 13px. Role editor (CqaSq): body gap 12, group gap 4, 11px group headings (0.55px tracking, 14px line height), 12px permission keys, 9px namespace tag, 16px checkboxes, 36px footer buttons with 16px padding. Confirm admin mapping (Kp48V): warning box padding 11px (108px tall). Restore backup (E9EEv0): source and target fields use the field tokens. Where the app was ruled right, the design changed instead (design-export/MANIFEST.md, 2026-09-19 entries). | Settled 2026-09-19 (rounds 5-7) |
| OD-22 | Visual-diff capture and comparison exceptions (test harness, not product). | Removable role chips XL5ZU, vStkb, uw0dB: per-frame thresholds 13% global / 38% block in `web/scripts/compare-screenshots.mjs`, their diff being text rasterization only (round 5). The Modpacks (tY6RD) capture drops the click's focus and hover state (round 5). `REFERENCE_CROP_ALLOWLIST` crops design exports that include drop-shadow bleed (zhLZN, DMnEi) to their opaque panel before scaling and diffing (round 6). DMnEi is captured at a 1440×1200 viewport so the whole form fits under the body's 80vh cap (round 6). MaoHP's password input is focused before capture (round 6, OD-16). Dialog captures (element is or sits inside `role=dialog`/`alertdialog`) drop up to 2 CSS px per edge of near-uniform modal backdrop; element crops keep the plain threshold-0 trim (`web/e2e/screenshots/capture.ts`, round 7). | Settled 2026-09-19 (rounds 5-7) |
| OD-23 | Design-file organisation of the six loose "J5pjJ3 states/*" frames (z6RDco Pending, WSAsQ Pool exhausted, UaEjg Pool not found, D76lH Address in use, IyMFM Tunnel enabled, jXykV Alerts). They are export-only, not diffed, and three of them sat among unrelated screens. | The four StatusRow copies become one reusable component family `Gameplane/Address Status Row/{Assigned, Pending, Pool exhausted, Pool not found, Address in use}` in the component area, mirroring the app's single `AddressStatusField` (`web/src/routes/tabs/settings/Networking.tsx`); J5pjJ3's own row becomes an instance of the Assigned variant and J5pjJ3 must render unchanged. Tunnel enabled becomes the screen variant `Screen/Server Detail — Settings · Networking (tunnel enabled)` directly right of J5pjJ3 (fallback: IyMFM moved there). Alerts (jXykV) moves directly below J5pjJ3. The emptied loose frames are deleted and the old→new ids recorded in design-export/MANIFEST.md; every touched node is re-exported in the same change (CLAUDE.md rule 1). Also, per OD-17, the footer buttons of the share-link dialogs atqRh, VM7ro and S7SCDc take 6px corners in the design (instance overrides only). | Settled 2026-09-19 (maintainer, in chat) |
| OD-24 | Share-link dialogs and keyboard focus, visual-diff round 8 (atqRh, VM7ro, S7SCDc; T207). | Create share link (atqRh): the "Allow starting the server" switch stays on the left, with its label and its explanation on separate lines (app); the design moves its switch to the left to match. The "Create link" button shows the chain-link icon before its label (app follows the design). Share link created (VM7ro): the app's current warning box stays (border-2 warning border, warning/10 fill, circled-! icon, bold heading) and the design is redrawn to match it; "Copy link" moves from inside the link box to an outlined button beside "Done" in the footer, the link box gets a border, and a small copy icon sits in the box's top-right corner (app follows the design). Revoke share link (S7SCDc): the app keeps its danger icon beside the title, as in the other danger confirmations; the design adds it. Keyboard (T207, SC-007): a focused table row keeps HeroUI's ring on its cells and also gets a row-level accent ring (a thicker, doubled ring). | Settled 2026-09-19 (maintainer, round 8) |
